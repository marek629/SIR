*** nearest release ***
  1 If zoom mode in preview dialog is "Fit to window size" resize window after
    image change (next, previous)
    * change flip icons
  2 Fix preview dialog resize bug (bigger than screen)
  3 Correction convert dialog
    * add external columns into tree view like size (px,k/MiB)
  ====== Piotrek ======
  ! optimalize MetadataUtils::Exif::flashString(short num)
    * try convert using << or >> operators
  ! seg. fault fix MetadataUtils::Metadata::setExifData() when Exif.Image.ImageWidth is empty
  =====================

*** next release ***
  1 Watermark support
  2 Correction convert dialog
