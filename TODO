*** nearest release ***
  ! MetadataDialog:
    * save the same time and date in Exif and IPTC
  ! Selection:
    * Dialog: add in exifGroupBox: FocalLegth, Aperture, ExpBias, IsoSpeed, ExpTime
  ! rotation patch
  ! Polish translation
    * iprove Pixele to Piksele in size unit combobox

*** next release ***
  ! Correction convert dialog
    * add external columns into tree view like size (px,k/MiB)
    * improve drag-drop events (myQTreeWidget)
  ! Conditional converting (ie. convert if image width > 1000 px)
    * scripts with exiv2 support
  ! Make recursive screen images with (auto detect of sceeen frame) feature - joke :)
  ! Make simple effects
    * color (sepia, b&w, etc.) and gradient filtres
    * watermark
    * add frame
