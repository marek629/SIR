*** nearest release ***
  1 If zoom mode in preview dialog is "Fit to window size" resize window after
    image change (next, previous)
    * change flip icons
  2 Correction convert dialog
    * add percentage (%) change image size possibility
    * add flip radio buttons near rotation line edit
    * add external columns into tree view like size (px,k/MiB)
    * add target size in bytes option
  ====== Piotrek ======
  ! add shutter speed in metadata dialog
  ! optimalize MetadataUtils::Exif::flashString(short num)
    * try convert using << or >> operators
  ! write slot for 'Delete metadata' button in metadata dialog
  =====================

*** next release ***
  1 Watermark support
  2 Correction convert dialog
